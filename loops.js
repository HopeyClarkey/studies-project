/**
 * LOOPS:
 * 
 * 0: (introduction)
 * 
 * 1) Explain while, for and for - in loops
 * 
 *    a while loop is a loop that executes with the keyword 'while', a parenthesis, and a condition that while true, whill continue to execute the block of code in the body of the while-loop.
 *    for loop is a loop that will move forward from an initialized index, to another index (found with a condition), and an increment statement
 *    for-in loop will pull all of the keys out of an Object, one by one. This allows you to use the key to get the values in the Object. You must use array syntax when accessing keys in an object.
 * 
 * 2) Be able to loop any number of  times, forward counting up to some limit, backward counting down to 0
 * 
 * 3) Loop over and Array, forwards and backwards
 * 
 * 4) Loop over an Object
 * 
 *
 */